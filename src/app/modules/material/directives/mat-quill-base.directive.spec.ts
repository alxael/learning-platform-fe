import { MatQuillBaseDirective } from './mat-quill-base.directive';

describe('MatQuillBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new MatQuillBaseDirective();
    expect(directive).toBeTruthy();
  });
});
